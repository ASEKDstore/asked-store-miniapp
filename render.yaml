services:
  # Frontend: Vite + React, билд как статический сайт
  - type: web
    name: asked-store-webapp
    runtime: static
    rootDir: webapp
    buildCommand: npm install && npm run build
    staticPublishPath: ./webapp/dist
    plan: free
    autoDeployTrigger: commit

  # Backend: Node + Express
  - type: web
    name: asked-store-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free
    autoDeployTrigger: commit
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 4000


