# ASKED Store (monorepo)

Чистая монорепа для Telegram Mini App магазина ASKED.

## Структура

- `webapp/` – фронтенд (Vite + React + TypeScript + Tailwind + React Router + Telegram WebApp)
- `backend/` – backend (Node + Express, TypeScript, каркас API)
- `shared/` – общие типы и утилиты
- `fixes/` – черновики и фикс-файлы, которые генерирует GPT (не участвуют в сборке)

## Версионирование

- Актуальная версия хранится в файле `VERSION`.
- История изменений — в `CHANGELOG.md`.
- Для автоматического инкремента версии есть скрипты:

```bash
npm run version:major # +1 по major
npm run version:minor # +1 по minor (по умолчанию для новых крупных фич)
npm run version:patch # +1 по patch (мелкие фиксы)
```

После внесения изменений:

1. Запусти нужный скрипт версии.
2. Обнови описание секции в CHANGELOG.md.
3. Сделай commit и tag, например: `v1.1.0`.

## Fixes / GPT-черновики

Все временные файлы, которые генерирует ИИ (альтернативные реализации, черновые фиксы и т.п.), должны лежать только в папке `fixes/`:

- `fixes/webapp/`
- `fixes/backend/`
- `fixes/shared/`

Эти файлы:
- не участвуют в сборке (исключены в `tsconfig.base.json`),
- по желанию могут быть исключены из git.

Рабочий код переносится вручную из `fixes/...` в соответствующие `src/`-файлы.

## Быстрый старт

```bash
# установка зависимостей
npm install

# запуск фронта
npm run dev:webapp

# запуск backend
npm run dev:backend
```

## 🔐 Admin Panel

Админка включается только если токены совпадают:

**Backend:** `ADMIN_TOKEN`  
**Frontend:** `VITE_ADMIN_TOKEN`

Пример:

```
ADMIN_TOKEN=ASKED_ADMIN_SUPER_SECRET_2025
VITE_ADMIN_TOKEN=ASKED_ADMIN_SUPER_SECRET_2025
```

Маршрут админки: `/admin`

Токен пробрасывается заголовком: `x-admin-token`.

## Deploy на Render

Используем `render.yaml` в корне.

- `asked-store-webapp` — статический фронтенд (Vite билд)
- `asked-store-backend` — Node + Express web service

**On Render:**
1. Подключить репозиторий.
2. Выбрать deploy по Blueprint (`render.yaml`).
3. Автодеплой включён по коммитам.

