# ASKED Store · Telegram Mini App

**ASKED Store** — мини-приложение магазина ASKED внутри Telegram.

- неоновый загрузочный экран с промокодами;
- главная с баннерами, топ-товарами, акциями и галереей;
- каталог, карточки товаров, корзина, оформление заказа;
- LIMITED ACCESS — цифровой пропуск с "магнитным ключом" и секретными дропами;
- админ-панель (товары, главная, промокоды, статистика, файлы, пользователи);
- уведомления в Telegram для модераторов;
- адаптация под Telegram WebApp (full-screen, safe-area, BackButton).

---

## 🧱 Tech stack

- **Frontend:** Vite, React, TypeScript, TailwindCSS  
- **Backend:** Node.js, Express, TypeScript  
- **Platform:** Telegram WebApp (`window.Telegram.WebApp`)  
- **Structure:** npm workspaces (`webapp`, `backend`, `shared`)

---

## 📁 Repo structure

```bash
asked-store/
  webapp/        # фронтенд (Vite + React + TS + Tailwind, Telegram Mini App)
  backend/       # API: каталог, заказы, limited-check, админка, Telegram уведомления
  shared/        # общие типы и утилиты (можно расширять)
  .github/       # GitHub Actions + templates
  package.json   # root, workspaces + общие scripts
  README.md
  CHANGELOG.md
```

---

## ⚙️ Local setup

**Требования:**
- Node.js >= 18
- npm >= 9

### 1. Установить зависимости

```bash
npm install
npm install --workspace webapp
npm install --workspace backend
```

### 2. Настроить окружение

**backend/.env**

```env
PORT=4000

ADMIN_TOKEN=ASKED_ADMIN_SUPER_SECRET_2025
OWNER_TELEGRAM_ID=123456789       # твой Telegram user id

TELEGRAM_BOT_TOKEN=xxxx:yyyy
TELEGRAM_ADMIN_CHAT_IDS=123456789 # один или несколько id через запятую
```

**webapp/.env**

```env
VITE_API_BASE_URL=http://localhost:4000

VITE_ADMIN_TOKEN=ASKED_ADMIN_SUPER_SECRET_2025
VITE_OWNER_TELEGRAM_ID=123456789

VITE_APP_VERSION=2.6.0
```

> `ADMIN_TOKEN` и `VITE_ADMIN_TOKEN` должны совпадать.  
> `OWNER_TELEGRAM_ID` — владелец, который видит кнопку Admin в меню.

### 3. Запуск dev-режима

Из корня репозитория:

```bash
npm run dev
```

Скрипт поднимет:
- **backend** на `http://localhost:4000`
- **webapp** на `http://localhost:5173`

---

## 🌐 Telegram Mini App

Фронт инициализируется через `Telegram.WebApp` (хук `useTelegram`):
- приложение растягивается на всю высоту экрана;
- учитываются safe-area от Telegram-клиента;
- используется `BackButton` для возврата на главную;
- если открыть проект не из Telegram — показывается fallback-страница с текстом "Открой через Telegram".

---

## 🔑 Limited Access

Экран Limited:
- принимает промокод / ключ доступа;
- отправляет его на `POST /limited/check`;
- backend проверяет код и возвращает:
  - данные пропуска (серия, партия, №, tier),
  - список секретных дропов;
- карта визуально "переворачивается", показывая NFT-like пропуск.

---

## 🧾 Orders

Экран оформления заказа:
- собирает данные: ФИО, телефон, адрес, тип доставки, комментарий;
- прикладывает содержимое корзины и Telegram-данные пользователя;
- отправляет на `POST /orders`;

**backend:**
- назначает номер заказа (`00001`, `00002`, ...),
- сохраняет статус,
- отправляет уведомление модераторам в Telegram.

---

## 🛠 Admin Panel

Админка доступна на `/admin` и защищена:
- по токену (`ADMIN_TOKEN` / `VITE_ADMIN_TOKEN`);
- по Telegram-идентификатору (`OWNER_TELEGRAM_ID` и роли).

**Разделы:**
- **Товары** — управление карточками каталога;
- **Главная** — баннеры, промо, галерея, соцсети;
- **Промокоды** — limited-коды и серии;
- **Статистика** — заказы, выручка, топ-товары (обновление каждые 10 сек);
- **Файлы** — загрузка и удаление картинок (file manager);
- **Пользователи** — список и роли (user / manager / admin).

---

## 🚀 Deploy

### Render: backend

**Service type:** Web Service

**Root Directory:** `backend`

**Build command:**

```bash
npm install && npm run build
```

**Start command:**

```bash
node dist/index.js
```

**Env vars:** перенести значения из `backend/.env`  
(кроме `PORT`, Render задаёт сам).

### Render: frontend

**Service type:** Static Site

**Root Directory:** `webapp`

**Build command:**

```bash
npm install && npm run build
```

**Publish directory:**

```bash
dist
```

**Env vars:**

```env
VITE_API_BASE_URL=https://<your-backend>.onrender.com
VITE_ADMIN_TOKEN=ASKED_ADMIN_SUPER_SECRET_2025
VITE_OWNER_TELEGRAM_ID=123456789
VITE_APP_VERSION=2.6.0
```

---

## 🤖 CI

Проект использует GitHub Actions:
- **ci.yml** — проверка сборки и линта при каждом push/PR;
- **deploy-render.yml** (опционально) — вызов Render Deploy Hook.

---

## 📜 License

Проект закрытый; лицензия не выбрана (по умолчанию все права защищены).

---

## 🧾 Changelog

Смотри [CHANGELOG.md](./CHANGELOG.md) для истории версий.

---

## ✨ Roadmap (идеи)

- реальная БД (PostgreSQL / Mongo) для заказов, пользователей и ролей;
- отдельный Admin Mini App с управлением через Telegram;
- персональные пуши / нотификации для limited-холдеров;
- интеграция с платёжными провайдерами.
